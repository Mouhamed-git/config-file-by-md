apiVersion: v1
kind: Service
metadata:
  name: your-service-name #reverseproxy-svc for exemple
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: your-deployment-app-name
